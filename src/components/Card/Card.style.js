import colors from '../../colors';

const style = {
  card: {
    padding: 0,
    marginBottom: 10,
    borderRadius: 15,
    backgroundColor: colors.white,
    overflow: 'hidden',
    borderWidth: 0
  }
};

export default style;
